import React from "react";

const SkillsForm = ({ state, stateHandler, addField, deleteField }) => {
  return <form className="form mx-auto p-6"></form>;
};

const FormGroup = ({ data, changeHandler, addField, deleteField, id }) => {
  <div className="bg-gray-50 p-6 rounded-lg my-6"></div>;
};
